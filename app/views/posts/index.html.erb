<h1>つぶやき一覧</h1>

<%= link_to '新しいつぶやき', new_post_path %>

<ul>
  <% @posts.each do |post| %>
    <li>
      <%= post.content %>
      <%= link_to '編集', edit_post_path(post) %>
      <%= link_to '削除', post_path(post), method: :delete %>
    </li>
  <% end %>
</ul>
